\begin{frame}{Положительная оценка: стек}
  \lead{Базовый путь закрывает минимум.} Swing и Android --- сверх него.

  \medskip
  \begin{columns}[T]
    \begin{column}{0.49\textwidth}
      \begin{block}{Обязательно}
        \begin{itemize}\setlength\itemsep{0.28em}
          \item REST API на Spring Boot;
          \item PostgreSQL;
          \item unit- и интеграционные тесты;
          \item logging;
          \item Docker и CI;
          \item web-клиент TypeScript / React
                (закрываем HTML/CSS).
        \end{itemize}
      \end{block}
    \end{column}
    \begin{column}{0.49\textwidth}
      \begin{block}{Сверх минимума}
        \begin{itemize}\setlength\itemsep{0.28em}
          \item Java Swing --- стандартный путь;
          \item Android / Kotlin --- продвинутый;
          \item React не вычёркивается:
                desktop и mobile --- рядом, не вместо web.
        \end{itemize}
      \end{block}
    \end{column}
  \end{columns}
\end{frame}
