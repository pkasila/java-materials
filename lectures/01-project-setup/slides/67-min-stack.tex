\begin{frame}{Положительная оценка: стек}
  \lead{Базовый путь закрывает минимум.} Стандартный добавляет Swing,
  продвинутый --- Android/Kotlin.

  \medskip
  \small
  \begin{columns}[T]
    \begin{column}{0.49\textwidth}
      \begin{block}{Обязательно}
        \begin{itemize}\setlength\itemsep{0.18em}
          \item REST API на Spring Boot;
          \item PostgreSQL;
          \item unit- и интеграционные тесты;
          \item logging;
          \item Docker и CI;
          \item web-клиент на TypeScript, HTML/CSS и React.
        \end{itemize}
      \end{block}
    \end{column}
    \begin{column}{0.49\textwidth}
      \begin{block}{Сверх минимума}
        \begin{itemize}\setlength\itemsep{0.18em}
          \item Java Swing --- стандартный путь;
          \item Android / Kotlin --- продвинутый;
          \item Web обязателен на всех путях и не заменяется
                desktop- или mobile-клиентом.
        \end{itemize}
      \end{block}
    \end{column}
  \end{columns}
\end{frame}
